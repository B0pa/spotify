{{ form_start(form, {'attr': {'class': 'd-flex'}}) }}

<div class="mb-3 w-100">
    {{ form_label(form.query, "Rechercher un morceau", {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.query, {
        'attr': {
            'class': 'form-control',
            'placeholder': 'Tapez le nom d\'un morceau...'
        }
    }) }}
</div>

{{ form_end(form) }}

