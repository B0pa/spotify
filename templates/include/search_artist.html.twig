{{ form_start(form, {'attr': {'class': 'd-flex'}}) }}

<div class="mb-3 w-100">
    {{ form_label(form.query, "Rechercher un artiste", {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.query, {
        'attr': {
            'class': 'form-control',
            'placeholder': 'Tapez le nom d\'un artiste...'
        }
    }) }}
</div>

{{ form_end(form) }}

